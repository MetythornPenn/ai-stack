# docker/Dockerfile.ui

FROM python:3.10-slim

WORKDIR /app

COPY ../requirements.txt .

RUN pip install --no-cache-dir -r requirements.txt

COPY ../ui ./ui

EXPOSE 7860

# Adjust `ui.main` if your Gradio script is named differently
CMD ["python", "ui/__init__.py"]
